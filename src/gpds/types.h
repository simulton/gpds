#pragma once

#include <string>

namespace Gpds
{
    class Container;

    // Value types
    using gBool      = bool;
    using gInt       = int;
    using gReal      = double;
    using gString    = std::string;
    using gContainer = Container*;
}
